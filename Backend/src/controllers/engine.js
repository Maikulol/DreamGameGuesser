// Processes the user input from the front end and provides output
exports.computeResult = async (req, res, next) => {
    try {
        //TODO: get data from frontend and compute game/list of games to show the user
    } catch (error) {
        next(error);
    }
}